#ifndef UTILS_H
#define UTILS_H

#include <raylib.h>

namespace Theme {
    const Color BG = GetColor(0x282c34);
    const Color BG_ALT = GetColor(0x3b4048);
    const Color FG = GetColor(0xabb2bf);
    const Color FG_ALT = GetColor(0x818896);
    const Color PRIMARY = GetColor(0xa55bd3aa);
    const Color SECONDARY = GetColor(0x5bd3aa);
    const Color TERTIARY = GetColor(0xd3aa5b);
}

#endif